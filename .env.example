# ✅ NEON + VERCEL CONFIGURATION
# This app runs ONLY on Neon PostgreSQL + Vercel Serverless

# ==============================================
# 🗄️ NEON DATABASE (REQUIRED)
# ==============================================
# Get from: https://neon.tech
# 1. Create a Neon account (free)
# 2. Create a new project: "royal-foods-erp"
# 3. Copy the connection string from dashboard
# Format: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# ==============================================
# 🔐 SESSION SECRET (REQUIRED)
# ==============================================
# Generate a random 32+ character string
# Use: openssl rand -base64 32
# Or online: https://generate-random.org/api-key-generator
SESSION_SECRET=your-very-long-random-secret-key-here-minimum-32-chars

# ==============================================
# 🌍 ENVIRONMENT (REQUIRED)
# ==============================================
# For Vercel deployment, set to: production
# For local dev, can be: development
NODE_ENV=production

# ==============================================
# 📝 NOTES FOR VERCEL DEPLOYMENT
# ==============================================
# 1. Copy these variables to Vercel:
#    Dashboard → Your Project → Settings → Environment Variables
#
# 2. Add each variable:
#    - DATABASE_URL (from Neon)
#    - SESSION_SECRET (generate new)
#    - NODE_ENV (set to "production")
#
# 3. Make sure DATABASE_URL includes "?sslmode=require" at the end
#
# 4. Never commit .env file to git!
#    (Already in .gitignore)

# ==============================================
# ✅ THAT'S IT!
# ==============================================
# No other environment variables needed.
# This app is optimized for:
# - Database: Neon PostgreSQL (serverless)
# - Hosting: Vercel (serverless functions)
# - Cost: FREE (both platforms have generous free tiers)
